# Deprecated: `den-controller`

`ee/apps/den-controller` has been replaced by `ee/apps/den-api` (`den-api`, formerly `den-controller`).

Use `ee/apps/den-api` for:

- auth endpoints
- org endpoints
- admin endpoints
- worker endpoints
- local and production Den control-plane work

This directory is intentionally kept as a stub so older links resolve to a clear migration note.
