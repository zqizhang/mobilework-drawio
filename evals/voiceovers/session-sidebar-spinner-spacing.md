# session-sidebar-spinner-spacing — Long active titles stay clear of the spinner

1. I start a task with a long name while it is active. The sidebar keeps the full name available, truncates it cleanly with an ellipsis, and puts the activity spinner on the left so the title never has to fight it for space on the right.
