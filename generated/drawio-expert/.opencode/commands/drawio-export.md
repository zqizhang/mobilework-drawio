---
description: 通过Docker HTTP Export Server导出PNG、JPEG或PDF。
agent: drawio-expert
---

调用TypeScript运行时工具drawio_validate；通过后调用drawio_export。只使用png、jpeg或pdf，确认返回文件非空且文件头有效，并报告输入、输出、格式、字节数和警告。不得调用Draw.io Desktop。
