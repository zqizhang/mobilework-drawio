---
description: 执行自动布局、路由调整和质量门禁。
agent: drawio-expert
subtask: true
---

若文件已经绑定，先调用drawio_get_state取得最新XML和revision。可先以dry_run=true调用drawio_polish核对前后评分及完整差异；仅在afterQuality.pass=true时携带准确base_revision正式调用drawio_polish，由工具弹出审批并在批准后写入。若存在活动批注，必须先取得diagram_wide审批，并把annotation_id和当前session的一次性approval_token传给正式drawio_polish。完成后必须调用drawio_finalize更新PNG；仅在shouldOpenBrowser=true时调用MobileWork工具openwork_browser_open_url，传入url=openUrl、provider="builtin"。
